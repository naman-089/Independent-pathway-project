rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(uid) {
      return isSignedIn() && request.auth.uid == uid;
    }

    function callerRole() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role;
    }

    function isCaseworker() { return isSignedIn() && callerRole() == "caseworker"; }
    function isAdmin()      { return isSignedIn() && callerRole() == "admin"; }
    function isStaff()      { return isCaseworker() || isAdmin(); }

    // ── Users ──────────────────────────────────────────────────────────────
    // Families read/write their own doc.
    // Staff can read all users.
    // Admins can also delete any user doc (used by the delete user feature).
    match /users/{uid} {
      allow read:   if isSignedIn(); // any signed-in user can read profiles (needed for DM discovery)
      allow create: if isOwner(uid);
      allow update: if isOwner(uid) || isAdmin();
      allow delete: if isAdmin() && request.auth.uid != uid; // can't delete yourself
    }

    // ── Intakes ────────────────────────────────────────────────────────────
    // Families read/write their own intake.
    // Staff can read all intakes.
    // Admins can delete any intake (cleanup when deleting a user).
    match /intakes/{uid} {
      allow read:   if isOwner(uid) || isStaff();
      allow create: if isOwner(uid);
      allow update: if isOwner(uid) || isStaff(); // caseworkers can update milestone verifications
      allow delete: if isOwner(uid) || isAdmin();
    }

    // ── Matches ────────────────────────────────────────────────────────────
    // Families read their own match.
    // Caseworkers/admins read & write all matches.
    // Admins can delete matches (cleanup when deleting a user).
    match /matches/{uid} {
      allow read:   if isOwner(uid) || isStaff();
      allow write:  if isStaff();
      allow delete: if isAdmin();
    }

    // ── Organizations ──────────────────────────────────────────────────────
    // Any signed-in user can read; only admins can create/update/delete.
    match /organizations/{orgId} {
      allow read:  if isSignedIn();
      allow write: if isAdmin();
    }

    // ── Community channel ──────────────────────────────────────────────────
    // Any signed-in user can read and post to the shared community channel.
    match /communityMessages/{msgId} {
      allow read, write: if isSignedIn();
    }

    // ── Direct messages ────────────────────────────────────────────────────
    // Any signed-in user can read/write DMs. The conversation ID is a
    // sorted pair of UIDs joined by "_", so only two people share each thread.
    // We allow any authenticated user here since the IDs are unguessable UUIDs.
    match /dms/{conversationId}/messages/{messageId} {
      allow read, write: if isSignedIn();
    }
  }
}
